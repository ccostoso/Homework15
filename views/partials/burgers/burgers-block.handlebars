<li class="list-group-item d-flex justify-content-between">
    {{burger_name}}

    <div>
        <button class="change-devoured btn btn-danger" data-id="{{id}}" data-devoured="{{devoured}}">
            Eat!
        </button>
        <button class="delete btn btn-danger" data-id="{{this.id}}">&#9746;</button>
    </div>
</li>